// RatingStars/RatingStars.jsx
import { View, Text, StyleSheet } from 'react-native';

const RatingStars = () => {
  return (
    <View style={styles.container}>
      <Text>RatingStars</Text>
    </View>
  );
};

export default RatingStars;

const styles = StyleSheet.create({
  container: { padding: 10 },
});